git clone https://github.com/dqnam268-vtt/adaptive-fractions-its1
cd adaptive-fractions-its1
py -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
py -m uvicorn app.main:app --reload

Lệnh này sẽ thông báo cho Git rằng bạn muốn bao gồm tệp Procfile vào lần commit tiếp theo của mình.
git add Procfile 

1. Mở giao diện tài liệu (Swagger UI)
Hãy mở trình duyệt web (Chrome, Firefox, v.v.) và truy cập vào địa chỉ sau:

http://127.0.0.1:8000/docs


cd ..	//Di chuyển ra khỏi thư mục hiện tại 

move "app\schemas\student_model.py" "app\core\"		
Lệnh này có nghĩa là: "di chuyển tệp student_model.py từ thư mục schemas đến thư mục core".

rmdir /s /q "app\api\__pycache__"
xóa cache